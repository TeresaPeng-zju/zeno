NEXT_PUBLIC_API_BASE=http://localhost:8000
# 设为 1 可在不启动后端时用本地 mock 数据预览完整流程（见 lib/mock.ts）。
NEXT_PUBLIC_USE_MOCK=0
# 结果页的「迁移护照」目前是本地预览，无需钱包/合约即可演示。
# 若日后要接真实链上铸造：部署 contracts/TransferPassport.sol 到 Base Sepolia，
# 把合约地址填这里，再接入 viem 钱包调用即可。
NEXT_PUBLIC_PASSPORT_ADDRESS=0x1De02cffe4540F5f1a673454D838549a0D0D1028
