"""Decision-surface comparison eval — Zeno vs external opponents.

This subpackage scores *learning sequences* (who-agnostic) so the deterministic
engine, a naive baseline, and DeepSeek can be compared on the same rulers:
hard constraints (dependency / prerequisite) + JD-grounded market alignment.
"""
