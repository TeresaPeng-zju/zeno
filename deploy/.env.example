# === Zeno Production Environment ===

# PostgreSQL
POSTGRES_USER=zeno
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=zeno

# API
CORS_ORIGINS=https://your-domain.com
LLM_PROVIDER=mock
OPENAI_API_KEY=
EMBEDDING_PROVIDER=mock

# Web
NEXT_PUBLIC_API_BASE=https://your-domain.com/api

# Data files (place these in apps/api/app/data/ before building)
# skill_graph.json, path_config.json, experience_capsules.json
