# Zeno · AI³（0G 赛道）Deck Prompt — 最小改动版

> 复用和鲸那份结构，仅微调"一句话"和第 8 页。整段复制粘贴给生成工具即可。无需截图。

```
帮我生成一份黑客松路演 PPT，共 12 页，横版 16:9，风格现代科技感、克制留白、深色底 + 一个高亮强调色（冷色科技蓝/紫），标题大字、每页要点不超过 4 条，关键术语保留英文。项目信息如下：

【项目名】Zeno：能力迁移导航 · AI Career Navigator
【一句话】用真实招聘数据诊断你的 AI 转型差距——诊断的表达层跑在 0G Compute 去中心化可验证推理上，结论可复现、生成可验证。
【定位】面向开发者与产品经理的 AI 职业迁移导航；Web3 + AI 产品，核心亮点是基于 0G Private Computer 的可验证推理。
【slogan】决策可复现，表达可验证。

请按以下页面结构生成，每页给出标题 + 要点文案 + 一句配图/视觉建议：

1. 封面：项目名、一句话简介、slogan、赛道「Build with 0G Private Computer」。
2. 问题：技术转型最难的不是缺学习资料，而是判断——哪些能力能迁移？真正缺什么？该先学哪个？现有工具要么恭维、要么淹没用户；且 AI 诊断多是黑箱，无从验证。
3. 解决方案：Zeno 只诚实做一件事——用真实招聘数据告诉你站在哪、哪些能力能带走、最有效的下一步。「只诊断，不承诺」。
4. 目标用户：想转 AI 的前端/后端/全栈工程师、学生，以及想转 AI PM 的产品经理。
5. 产品流程（3 步）：① 交互式"能力星图"点亮可迁移的工程基础，能力链 TypeScript→API→流式→Prompt→工具调用→Agent；② 随滚动推进的"迁移地图"，给出可迁移优势、真实差距（核心/加分）、按优先级排序且配好学习资源的路线图；③ 贴一段真实招聘 JD，自动识别偏向 RAG/Agent/评估并重新校准。
6. 核心差异化——决策与表达分离：确定性引擎跑在真实招聘数据校准的 23 技能图谱上，算优势/差距/依赖/优先级，结果可复现；LLM 只把诊断翻译成人话，绝不参与判断。配一张"引擎算 → LLM 说"的对比图。
7. 技术架构：前端 Next.js 15 + React Flow；后端 FastAPI；数据库 Postgres 16 + pgvector；RAG 资源召回（向量召回 + 多信号重排）；表达层推理由 0G Compute（Router，OpenAI 兼容）提供。给一张分层架构图。
8. ★核心亮点：0G Compute 可验证推理：正因为表达层被隔离出来，我们把它整体接到 0G Private Computer——通过官方 OpenAI 兼容 Router 调用去中心化模型。每次生成返回可链上验证的 request id、由 TEE 背书，结果页实时显示验证徽章。决策可复现 + 表达可验证 = 高可信职业诊断。（次要卖点：评估后还可一键生成一枚 Soulbound / ERC-5192 不可转让的迁移护照，链上现场渲染 SVG。）
9. 为什么这很重要：AI 给的职业建议不该是黑箱；去中心化 + 可验证推理让诊断的每一句都可追溯、不被单一平台垄断或篡改。
10. 现状与开源：已有可运行 Demo，支持 English/简体中文/繁體中文；Apache 2.0 开源，Open Core，自带示例技能图谱开箱即用；GitHub + 完整 README。
11. 如何满足赛题：✓ 集成 0G Private Computer 模型（官方 API 调用）；✓ 可运行 Web App Demo；✓ GitHub + README；✓ 3 分钟演示视频；✓ 体现可验证推理价值；✓ 结合 Web3（链上 SBT）。
12. 结尾：一句话愿景 + 仓库地址 github.com/TeresaPeng-zju/zeno + Demo 链接占位 + 联系人。

可用星图、节点连线、TEE/链上验证/徽章等视觉意象。文案中英混排以中文为主。
```
